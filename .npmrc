# 預設官方來源；在企業網路可用 NPM_CONFIG_REGISTRY 覆寫
registry=https://registry.npmjs.org/

# 減少在受限網路下額外請求
fund=false
audit=false

# 建議：若外網受限，請改用內部 registry
# npm config set registry https://<your-internal-registry>
# 或單次：NPM_CONFIG_REGISTRY=https://<your-internal-registry> npm install
